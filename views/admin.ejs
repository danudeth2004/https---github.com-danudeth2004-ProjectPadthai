<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders Customer</title>
    <link rel="stylesheet" href="/stylesheets/style_admin.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css' rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Rowdies:wght@300;400;700&display=swap">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cousine:ital,wght@0,400;0,700;1,400;1,700&family=Gilda+Display&family=Krub:ital,wght@0,200;0,300;0,400;0,500;0,600;0,700;1,200;1,300;1,400;1,500;1,600;1,700&family=Protest+Strike&family=Rowdies&family=Shippori+Mincho&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>
    <div class="container">
      <form method="post">
        <h1>Orders Customer</h1>
        <ul class="responsive-table">
          <% var end = 1; %>
          <% for(var i=0;i < price.length;i++) { %>
            <% if(price[i].status == 0) { %>
              <% end = 0; %>
            <% } %>
          <% } %>
          <% if(end == 1) { %>
            <li class="table-header">
              <div class="col col-1">Order No.</div>
              <div class="col col-2">Detail no.</div>
              <div class="col col-3">Noodles type</div>
              <div class="col col-4">Meat type</div>
              <div class="col col-5">Topping</div>
              <div class="col col-6">Vegetable</div>
            </li>
          <% } %>
          <% var temp = min[0].order_min; %>
          <% for(var i=0;i < price.length;i++) { %>
            <% if(price[i].status == 0) { %>
              <li class="table-header">
                <div class="col col-1">Order No.</div>
                <div class="col col-2">Detail no.</div>
                <div class="col col-3">Noodles type</div>
                <div class="col col-4">Meat type</div>
                <div class="col col-5">Topping</div>
                <div class="col col-6">Vegetable</div>
              </li>
              <% var id = 1; %>
              <% for(var j=0;j < submit.length;j++) { %>
                <% if(submit[j].order_id == price[i].order_id) { %>
                  <li class="table-row" data-index="<%= i %>">
                    <div class="col col-1" data-label="Order No.">
                      <% if(submit[j].order_id == temp) { %>
                        <%= submit[j].order_id %>
                        <% temp++; %>
                      <% } %>
                    </div>
                    <div class="col col-2" data-label="Detail no."><%= id %></div>
                    <% id++; %>
                    <div class="col col-3" data-label="Noodles type">
                      <% if(submit[j].Detail_id == detail[j].Detail_id) { %>
                        <% if(detail[j].noodles_nameEN === null) { %>
                          <%= detail[j].noodles_nameTH %>
                        <% } else { %>
                          <%= detail[j].noodles_nameEN %>
                        <% } %>
                      <% } %>
                    </div>
                    <div class="col col-4" data-label="Meat type">
                      <% if(submit[j].Detail_id == detail[j].Detail_id) { %>
                        <% if(detail[j].meat_nameEN === null) { %>
                          <%= detail[j].meat_nameTH %>
                        <% } else { %>
                          <%= detail[j].meat_nameEN %>
                        <% } %>
                      <% } %>
                    </div>
                    <div class="col col-5" data-label="Topping">
                      <% if(submit[j].Detail_id == detail[j].Detail_id) { %>
                        <% if(detail[j].topping_nameEN === null) { %>
                          <%= detail[j].topping_nameTH %>
                        <% } else { %>
                          <%= detail[j].topping_nameEN %>
                        <% } %>
                      <% } %>
                    </div>
                    <div class="col col-6" data-label="Vegetable">
                      <% if(submit[j].Detail_id == detail[j].Detail_id) { %>
                        <% if(detail[j].veg_nameEN === null) { %>
                          <%= detail[j].veg_nameTH %>
                        <% } else { %>
                          <%= detail[j].veg_nameEN %>
                        <% } %>
                      <% } %>
                    </div>
                  </li>
                <% } %>
              <% } %>
          <form method="post">
          <input type="hidden" name="indexId" value="<%= i+1 %>">
          <li class="table-footer">
            <div class="col col-7">Total Price&nbsp;&nbsp;<%= price[i].total_price %>฿</div>
            <div class="col col-8">Payment Status&nbsp;&nbsp;<input type="checkbox" class="ui-checkbox"></div>
              <div class="col col-9">
                <button type="submit" class="cta">
                  <span>Serve Already</span>
                  <svg width="15px" height="10px" viewBox="0 0 13 10">
                    <path d="M1,5 L11,5"></path>
                    <polyline points="8 1 12 5 8 9"></polyline>
                  </svg>
                </button>
              </div>
          </li>
          </form>
            <% } %>
          <% } %>
        </ul>
    </div>
</body>
</html> 